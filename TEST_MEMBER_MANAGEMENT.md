# 成员管理功能测试指南

## 📋 功能概述

成员管理功能允许聊天室管理员添加新成员、移除现有成员、修改成员角色。

## ✅ 已实现功能

### 1. **添加成员**
- 管理员点击"添加成员"按钮
- 搜索用户（昵称或邮箱，至少2个字符）
- 防抖搜索（300ms延迟）
- 自动排除已加入成员
- 显示用户信息：头像、昵称、邮箱、签名
- 点击"添加"按钮邀请用户

### 2. **移除成员**
- 管理员可以移除普通成员
- 保护机制：不能移除创建者
- 确认对话框（防止误操作）
- Toast通知反馈

### 3. **修改角色**
- 设为管理员 / 取消管理员
- 确认对话框
- 保护机制：不能修改创建者角色
- 角色标签实时更新

### 4. **成员列表**
- 网格布局（1-3列响应式）
- 显示成员信息：头像、昵称、签名、加入时间
- 管理员标签
- 创建者标识（👑 图标）

## 🧪 测试步骤

### 准备工作
1. 注册至少3个测试账号：
   - 账号A：聊天室创建者
   - 账号B：普通成员
   - 账号C：待邀请用户

2. 使用账号A创建一个测试聊天室

### 测试用例 1：添加成员
1. 用账号A登录，进入聊天室详情页
2. 切换到"成员"标签
3. 点击"添加成员"按钮
4. 输入账号C的昵称或邮箱（至少2个字符）
5. 等待搜索结果（300ms延迟）
6. 验证账号C出现在搜索结果中
7. 验证已加入的成员（账号A、B）不出现
8. 点击"添加"按钮
9. 验证 Toast 提示"已添加 [昵称]"
10. 验证成员列表刷新，显示新成员

### 测试用例 2：设为管理员
1. 用账号A登录，进入聊天室详情页
2. 切换到"成员"标签
3. 找到账号B的成员卡片
4. 点击"设为管理员"按钮
5. 确认对话框中点击"确定"
6. 验证 Toast 提示"已设为管理员"
7. 验证账号B的卡片显示"管理员"标签
8. 验证按钮文字变为"取消管理员"

### 测试用例 3：取消管理员
1. 继续上一步，点击"取消管理员"
2. 确认对话框中点击"确定"
3. 验证 Toast 提示"已取消管理员"
4. 验证"管理员"标签消失
5. 验证按钮文字变回"设为管理员"

### 测试用例 4：移除成员
1. 用账号A登录，进入聊天室详情页
2. 找到账号C的成员卡片
3. 点击"移除"按钮
4. 确认对话框中点击"确定"
5. 验证 Toast 提示"成员已移除"
6. 验证成员列表刷新，账号C消失
7. 验证成员数量更新（顶部显示）

### 测试用例 5：保护机制
1. 用账号A（创建者）登录
2. 验证自己的卡片**没有**操作按钮
3. 验证其他管理员**无法**修改或移除创建者
4. 切换到账号B（管理员）登录
5. 验证账号B可以看到"添加成员"按钮
6. 验证账号B可以管理其他普通成员
7. 验证账号B**无法**管理创建者（账号A）

### 测试用例 6：权限验证
1. 用账号C（普通成员）登录
2. 进入聊天室详情页
3. 验证**看不到**"添加成员"按钮
4. 验证**看不到**"⚙️ 设置"标签
5. 验证成员卡片**没有**操作按钮

### 测试用例 7：搜索功能
1. 用管理员账号登录
2. 点击"添加成员"
3. 输入1个字符 → 验证提示"请输入至少2个字符"
4. 输入2个字符 → 验证开始搜索
5. 输入不存在的昵称 → 验证显示"未找到匹配的用户"
6. 输入邮箱的一部分 → 验证也能搜索到
7. 快速输入多个字符 → 验证防抖生效（只发送最后一次）

### 测试用例 8：响应式布局
1. 桌面端（≥1024px）→ 验证成员卡片3列布局
2. 平板端（768-1023px）→ 验证成员卡片2列布局
3. 移动端（<768px）→ 验证成员卡片1列布局
4. 验证对话框在各尺寸下正常显示

### 测试用例 9：Dark Mode
1. 切换到暗色模式
2. 验证成员列表样式正常
3. 验证添加成员对话框样式正常
4. 验证按钮、标签颜色正确

## 🐛 已知问题

暂无已知问题。

## 📁 相关文件

- **UI组件**：`src/app/chat/[roomId]/page.tsx`
- **对话框**：`src/components/AddMemberDialog.tsx`
- **API函数**：`src/lib/chatRoomApi.ts`
  - `addRoomMember(roomId, userId)` - 添加成员
  - `removeRoomMember(roomId, userId)` - 移除成员
  - `updateMemberRole(roomId, userId, role)` - 修改角色
  - `getRoomMembers(roomId)` - 获取成员列表

## 🎯 下一步计划

- [ ] WebSocket 服务器搭建（需求3）
- [ ] 实时消息功能（需求4+5）
- [ ] 编辑聊天室信息
- [ ] 删除聊天室功能

## 💡 使用提示

1. **搜索性能**：防抖延迟设置为300ms，平衡了响应速度和API调用次数
2. **权限设计**：管理员拥有所有成员管理权限，但无法修改创建者
3. **用户体验**：所有危险操作（移除、修改角色）都有确认对话框
4. **实时更新**：操作成功后自动刷新成员列表，无需手动刷新页面

---

测试愉快！如有问题请及时反馈。
